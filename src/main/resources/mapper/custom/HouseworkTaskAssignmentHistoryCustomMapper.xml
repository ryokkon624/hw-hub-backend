<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hwhub.backend.infrastructure.mybatis.custom.mapper.HouseworkTaskAssignmentHistoryCustomMapper">
    <insert id="insertClearedAssigneeHistory">
        INSERT
        INTO t_housework_task_assignment_history(
                                                  housework_task_id
                                                , household_id
                                                , from_assignee_user_id
                                                , to_assignee_user_id
                                                , operated_user_id
                                                , assign_reason_type
                                                , note
                                                , create_user_id
                                                , create_program
                                                , update_user_id
                                                , update_program
        )
        SELECT
            housework_task_id
             , household_id
             , assignee_user_id
             , NULL
             , #{userId}
             , '9'
             , 'メンバーが世帯から離脱したため'
             , #{userId}
             , #{program}
             , #{userId}
             , #{program}
        FROM
            t_housework_task
        WHERE
            household_id = #{householdId}
          AND assignee_user_id = #{assignee}
          AND status = '0'
    </insert>
</mapper>
