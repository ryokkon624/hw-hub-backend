<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hwhub.backend.infrastructure.mybatis.custom.mapper.UserCustomMapper">

    <select id="findPasswordChangedAt" resultType="java.time.LocalDateTime">
        SELECT password_changed_at
        FROM m_user
        WHERE user_id = #{userId}
    </select>

    <update id="updateAuthProvider">
        UPDATE
            m_user
        SET
            auth_provider = #{authProvider},
            auth_provider_id = #{authProviderId},
            update_user_id = #{updateUserId},
            update_program = #{programTypeCode}
        WHERE
            user_id = #{userId}
    </update>

    <update id="linkGoogleAccount">
        UPDATE
            m_user
        SET
            auth_provider = #{authProvider},
            auth_provider_id = #{authProviderId},
            email = #{email},
            display_name = #{displayName},
            password_hash = NULL,
            password_changed_at = NOW(6),
            email_verified_at = IF(email_verified_at IS NULL, NOW(6), email_verified_at),
            update_user_id = #{userId},
            update_program = #{program}
        WHERE
            user_id = #{userId}
    </update>
</mapper>
